{% extends 'base.html.twig' %}

    {#Home users#}
    <div class="container">
        {% block title %}Users{% endblock %}

        {# Block body #}
        {% block body %}
            <span>Mes utilisateurs!</span>
            {% include "_partials/_items.html.twig" %}
            {% include "_partials/_items.html.twig" %}
            {% include "_partials/_items.html.twig" %}
            <p>
                {{ user.name }}
                {{ user.lastName }}
                {{ user.mail }}
            </p>

        {% endblock %}

    </div>
